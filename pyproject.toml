[project]
name = "pullback-detector-v3"
version = "0.1.0"
description = "High-quality intraday pullback detection engine for sideways and transitional markets."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
